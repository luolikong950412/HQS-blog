#冒号后面需要一个空格
server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/java_db
    username: root
    password: huqingshan
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  #路径
  mapper-locations: classpath:mapper/*.xml
  #包,实体类，不是mapper
  type-aliases-package: com.hello.huqingshan.model